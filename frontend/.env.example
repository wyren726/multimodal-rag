# Environment Configuration for Multimodal RAG Frontend

# Use mock API for development/testing (true/false)
# When true, uses mock data instead of real backend API
VITE_USE_MOCK_API=true

# Backend API Base URL (only used when VITE_USE_MOCK_API=false)
# Default: http://localhost:8000
# VITE_API_BASE_URL=http://localhost:8000
